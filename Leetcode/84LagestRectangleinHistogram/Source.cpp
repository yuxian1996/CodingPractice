#include <vector>
#include <algorithm>
using namespace std;

int main()
{
	vector<int> heights;

	vector<int> maxRects(heights.size(), 0);
	int maxRect = heights[0];
	maxRects[0] = heights[0];

	for (int i = 1; i < heights.size(); ++i)
	{
		
	}

}